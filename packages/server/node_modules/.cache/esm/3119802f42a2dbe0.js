let gql;_42a‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers]]);_42a‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
    type Demand implements Node {
        id: ID!
        name: String!
        client: Client!
        deadline: String
    }
    extend type Query {
        demands: [Demand]!
    }
`;

       const resolvers = {
    Query: {
        demands: () => [],
    },
};